{
  "name": "drivetaxi-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "Сбор данных Google Ads + Meta Ads + amoCRM и сборка data.json для дашборда",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch:google": "node scripts/fetch-google-ads.js",
    "fetch:meta": "node scripts/fetch-meta-ads.js",
    "fetch:amocrm": "node scripts/fetch-amocrm.js",
    "build": "node scripts/build-data.js",
    "update": "npm run fetch:google && npm run fetch:meta && npm run fetch:amocrm && npm run build"
  }
}
